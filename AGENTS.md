# HSK 4 Writing

- Read `docs/PROJECT_MAP.md` before changing application files and keep it current.
- Preserve saved workbooks and compatible JSON backups.
- Add user-facing messages in both `src/hsk/locales/en.ts` and `src/hsk/locales/vi.ts`.
- Keep each set printable as two clean portrait A4 pages (10 word-order questions and 5 picture prompts).
- Run `npm run build`, `npm run lint`, and appropriate browser/print checks after changes.
